apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: janik-kustomize-app
spec:
  tls:
    termination: edge
  to:
    name: janik-kustomize-app
    kind: Service
    weight: 100
  wildcardPolicy: None